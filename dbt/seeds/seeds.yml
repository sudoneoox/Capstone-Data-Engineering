version: 2

seeds:
  #INFO: ONET SEEDS
  # Category reference for stg_onet__education_training_experience
  # Sourced from https://www.onetcenter.org/dictionary/27.1/text/ete_categories.html
  - name: seed_onet__ete_categories
    description: "Category reference for onet education_training_experience"
    config:
      schema: raw

  - name: us_cities_states_counties
    description: "80k+ entries filled in with US cities, states, and counties"
    config: 
      schema: raw
      delimiter: "|"
